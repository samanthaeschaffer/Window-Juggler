# Run using command:
# python window-juggler.py sample_config.txt

## IMPORTANT ##
# Remove all comment lines when actually executing
# Script does not parse out comments

chromium-browser https://github.com/mate-desktop/ --new-window
chromium-browser https://developer.gnome.org/libwnck/stable/overview.html --new-window
mate-terminal
